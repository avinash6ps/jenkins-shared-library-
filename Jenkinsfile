@Library('my-shared-library') _
pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                exampleFunction()  // Calling a function from vars/exampleFunction.groovy
            }
        }
    }
}
